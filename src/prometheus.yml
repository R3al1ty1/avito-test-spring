global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'pvz-api'
    static_configs:
      - targets: ['pvz-backend:8080']  # Используем основной порт FastAPI
    metrics_path: '/metrics'